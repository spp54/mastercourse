let orderCount = 0

const takeorder = (topping1, topping2, ) => {
    console.log (`Order count = ${orderCount}`)
    console.log (`pizza with ${topping1} and ${topping2}`)
    orderCount++
   
}
takeorder("tomato", "beef")
takeorder("tomato", "beef")
takeorder("tomato", "beef")

